#include <iostream>
#include <cstdlib>
#include <assert.h>
using namespace std;

/*
bool ok(int q[], int col){
    for(int i=0; i<col; i++)
        if(q[col]==q[i] || (col-i)==abs(q[col]-q[i])) return false;
    return true;
};

int main(){
    int a[8]={1, 2, 3, 4, 5, 6, 7, 0};
    int b[8]={2, 2, 1, 7, 8, 5, 3, 4};
    int c[8]={7, 3, 0, 2, 5, 1, 6, 4};
    assert(!(ok(a,3)));
    assert(!(ok(b,7)));
    assert(ok(c,7));
    return 0;
}
*/
